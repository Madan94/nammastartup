import {database,getCompany} from '@/lib/server/repository';
import {correctionSchema} from '@/lib/catalog/validation';
import {apiError,rateLimit,readJson,requireSameOrigin} from '@/lib/server/http';
export async function POST(request:Request){try{requireSameOrigin(request);if(!await rateLimit(request,'correct'))return Response.json({error:'Too many requests. Try again later.'},{status:429});const data=correctionSchema.parse(await readJson(request));const company=await getCompany(data.companySlug);if(!company)return Response.json({error:'Company not found.'},{status:404});const id=crypto.randomUUID(),now=new Date().toISOString();await(await database()).run('INSERT INTO submissions (id,kind,name,website,email,payload,status,note,created_at,updated_at) VALUES (?,?,?,?,?,?,?,?,?,?)',[id,'correction',company.name,company.website,data.email,JSON.stringify(data),'pending','',now,now]);return Response.json({id,status:'pending'},{status:201});}catch(error){return apiError(error)}}
