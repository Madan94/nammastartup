import {describe,it,expect} from 'vitest';
import {publicUrl,companySchema,submissionSchema} from './validation';
import {verifiedCompanies} from '@/data/chennai';
describe('public data boundaries',()=>{it('rejects private addresses and active URL schemes',()=>{for(const url of ['javascript:alert(1)','http://company.com','https://127.0.0.1/','https://10.0.0.1','https://[::1]/','https://service.internal','https://user:pass@company.com'])expect(publicUrl.safeParse(url).success,url).toBe(false)});it('accepts reviewed bootstrap records',()=>{for(const company of verifiedCompanies)expect(companySchema.safeParse(company).success,company.name).toBe(true)});it('rejects a falsely precise map location',()=>{expect(companySchema.safeParse({...verifiedCompanies[0],latitude:null,longitude:null,locationPrecision:'office'}).success).toBe(false)});it('requires consent and rejects bot trap input',()=>{const c=verifiedCompanies[0];const data={...c,email:'person@company.com',careersUrl:c.careersUrl,consent:true};expect(submissionSchema.safeParse(data).success).toBe(true);expect(submissionSchema.safeParse({...data,consent:false}).success).toBe(false);expect(submissionSchema.safeParse({...data,websiteConfirm:'bot'}).success).toBe(false)});});
