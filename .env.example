NEXT_PUBLIC_APP_URL=http://localhost:3000
# Generate a private random value of at least 32 characters before using /admin.
ADMIN_ACCESS_KEY=
# Optional local SQLite location; ignored by the hosted D1 adapter.
# DATABASE_PATH=.data/chennai.sqlite
